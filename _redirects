# Cloudflare Pages already serves <name>.html at /<name> automatically, so
# the old Netlify rules for wishlist/data/sales/ma-compare/d2d are redundant
# — and conflicting with that default is what caused the redirect loop.
# Only this one stays: /movies has no movies.html file backing it, it's an
# alias for the Collection page which actually lives in index.html.
/movies   /index.html   200
