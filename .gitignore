# Generated by scripts/build-data.mjs on every deploy. Netlify rebuilds these
# from the live sheet, so committing them would only ever ship stale data.
snapshots/
mirror/
data-manifest.json
node_modules/
